import place from './place';
import placeAfter from './placeAfter';
import placeBefore from './placeBefore';
import placeFirst from './placeFirst';
import placeLast from './placeLast';
import replace from './replace';

const notPlacers = {
	place: place,
	placeAfter: placeAfter,
	placeBefore: placeBefore,
	placeFirst: placeFirst,
	placeLast: placeLast,
	replace: replace
};

export default notPlacers;
